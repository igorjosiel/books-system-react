import styled from 'styled-components';

const Icon = styled.li`
    margin-right: 40px;
    width: 25px;
`;

const Icons = styled.ul`
    display: flex;
    align-items: center;
`;

export { Icon, Icons };